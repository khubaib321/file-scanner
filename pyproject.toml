[project]
name = "file-scanner"
version = "0.1.0"
description = "Scan a directory. Discover files and folders 🤯"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.8.1",
    "pydantic>=2.11.7",
]
